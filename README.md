# LAW-AI: المساعد القانوني المصري ⚖️

هذا المشروع تطبيق ويب بسيط مبني باستخدام **Streamlit** و **Google Gemini AI** لمساعدة المستخدمين في العثور على معلومات قانونية مصرية.
This project is a simple web app built with **Streamlit** and **Google Gemini AI** to help users find Egyptian legal information.

## 📂 بنية المشروع | Project Structure

```
LAW-AI/
├── app.py              # الصفحة الرئيسية (Landing Page)
├── users.json          # قاعدة بيانات المستخدمين (بسيطة)
├── requirements.txt    # الملفات المطلوبة
├── .env                # مفاتيح التشغيل (API Keys)
├── pages/
│   ├── auth.py         # صفحة الدخول والتسجيل
│   └── chat.py         # صفحة المحادثة مع المساعد الذكي
├── services/           # (Helper Code) الأكواد المساعدة
│   ├── llm_service.py      # التعامل مع الذكاء الاصطناعي
│   └── document_service.py # قراءة ملفات PDF
```

## 🚀 كيفية التشغيل | How to Run

1. **تثبيت المكتبات | Install Libraries**:
   افتح الشاشة السوداء (Terminal) واكتب:
   ```bash
   pip install -r requirements.txt
   ```

2. **إعداد المفاتيح | Setup Keys**:
   - تأكد من وجود ملف `.env`
   - يجب أن يحتوي على: `GOOGLE_API_KEY=your_key_here`

3. **تشغيل التطبيق | Run App**:
   ```bash
   streamlit run app.py
   ```

## 🛠️ تفاصيل الملفات | Files Details

- **app.py**: واجهة البداية، تشرح فكرة المشروع.
- **pages/auth.py**: نظام بسيط لتسجيل الدخول وإنشاء حساب وحفظ البيانات في ملف `users.json`.
- **pages/chat.py**: الشات البسيط، يرفع ملف PDF ويتحدث معه.
